import type { Locale } from '~/shared/config/i18n'

// Full endonyms for the dropdown rows; the trigger button shows the bare code.
export const LOCALE_LABELS: Record<Locale, string> = {
  en: 'English',
  fr: 'Français',
  ru: 'Русский',
}
