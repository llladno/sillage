pre-commit:
  parallel: true
  jobs:
    - name: eslint
      glob: '*.{js,ts,vue,mjs}'
      run: pnpm eslint --fix {staged_files}
      stage_fixed: true
    - name: prettier
      glob: '*.{js,ts,vue,mjs,json,css,md}'
      run: pnpm prettier --write {staged_files}
      stage_fixed: true
