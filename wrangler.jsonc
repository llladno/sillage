{
  // Cloudflare Pages project for the SILLAGE static site.
  // Deploy with `pnpm deploy` (runs `nuxt generate` then `wrangler pages deploy`).
  "name": "sillage-encre",
  "compatibility_date": "2026-09-01",
  "pages_build_output_dir": ".output/public"
}
